<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <title>Новости дома</title>

  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="description" content="Оповещения и новости для жильцов дома">

  <style>
    body {
      font-family: Arial, sans-serif;
      max-width: 700px;
      margin: 40px auto;
      padding: 0 16px;
      line-height: 1.6;
      background: #f9f9f9;
      color: #222;
    }

    h1 {
      font-size: 28px;
      margin-bottom: 10px;
    }

    .subtitle {
      color: #666;
      margin-bottom: 30px;
    }

    .news-list {
      background: white;
      padding: 20px;
      border-radius: 8px;
    }

    .news-item {
      margin-bottom: 16px;
      padding-bottom: 12px;
      border-bottom: 1px solid #eee;
    }

    .news-item:last-child {
      border-bottom: none;
    }

    a {
      text-decoration: none;
      color: #1a73e8;
      font-weight: bold;
    }

    a:hover {
      text-decoration: underline;
    }

    .date {
      font-size: 13px;
      color: #777;
      margin-top: 4px;
    }

    .loading {
      color: #777;
    }
  </style>
</head>

<body>

  <h1>Новости дома</h1>
  <div class="subtitle">Актуальные объявления и уведомления для жильцов</div>

  <div id="news" class="news-list">
    <div class="loading">Загрузка новостей...</div>
  </div>

  <script>
    const RSS_URL = "https://twiksoid.github.io/Homier/rss.xml";

    async function loadRSS() {
      try {
        const response = await fetch(RSS_URL);
        const text = await response.text();

        const parser = new DOMParser();
        const xml = parser.parseFromString(text, "text/xml");

        const items = xml.querySelectorAll("item");

        const container = document.getElementById("news");
        container.innerHTML = "";

        items.forEach(item => {
          const title = item.querySelector("title")?.textContent || "Без названия";
          const link = item.querySelector("link")?.textContent || "#";
          const pubDate = item.querySelector("pubDate")?.textContent || "";

          const date = new Date(pubDate);
          const formattedDate = isNaN(date)
            ? pubDate
            : date.toLocaleDateString("ru-RU", {
                year: "numeric",
                month: "long",
                day: "numeric"
              });

          const div = document.createElement("div");
          div.className = "news-item";

          div.innerHTML = `
            <a href="${link}">${title}</a>
            <div class="date">${formattedDate}</div>
          `;

          container.appendChild(div);
        });

      } catch (e) {
        document.getElementById("news").innerHTML =
          "<div class='loading'>Ошибка загрузки новостей</div>";
        console.error(e);
      }
    }

    loadRSS();
  </script>

</body>
</html>
